import styled from "styled-components";
import FlexColumnComponent from "../general/FlexColumnComponent";

const Info = styled(FlexColumnComponent)`
  justify-content: space-between;
  max-width: 940px;
  gap: 40px;
`;

export default Info;
