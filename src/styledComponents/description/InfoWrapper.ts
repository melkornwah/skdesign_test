import styled from "styled-components";
import FlexColumnComponent from "../general/FlexColumnComponent";

const InfoWrapper = styled(FlexColumnComponent)`
  gap: 20px;
`;

export default InfoWrapper;
